# Credentials — never commit
.credentials.json
settings.json
settings.local.json

# Conversation & session state
history.jsonl
mcp-needs-auth-cache.json
paste-cache/
session-env/
shell-snapshots/
backups/

# Runtime & cache
cache/
plugins/
projects/
file-history/
downloads/

# Research workspace — user-created projects (runtime, not config)
research/projects/

# Community skills installed by npx skills into research workspace (runtime binaries)
research/.claude/skills/*/

# Community skills symlinked into ~/.claude/skills/ by npx skills --global
# gemini-web-fetch is the only custom skill tracked here; others are community-installed
skills/brainstorming
skills/prd-development
skills/writing-plans
skills/verification-before-completion

# OS
.DS_Store
